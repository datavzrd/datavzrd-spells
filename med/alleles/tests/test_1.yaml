name: Test

datasets:
    test:
      path: data1.tsv
      separator: "\t"

views:
  test:
    dataset: test
    render-table:
      columns:
        some clinical column containing alleles:
          spell:
            url: ../spell.yaml
