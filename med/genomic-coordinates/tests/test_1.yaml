name: Test

datasets:
    test:
      path: data1.tsv
      separator: "\t"

views:
  test:
    dataset: test
    render-table:
      columns:
        some column with genomic coordinates:
          spell:
            url: ../spell.yaml
