datasets:
  test:
    path: data1.tsv
    separator: "\t"

views:
  test:
    dataset: test
    render-table:
      columns:
        some boolean column:
          spell:
            url: ../spell.yaml
            with:
              true_value: "true"
              false_value: "false"